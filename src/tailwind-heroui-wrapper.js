import { heroui } from "@heroui/theme";

/**
 * Tailwind v4 @plugin directive expects a default export.
 * Exporting the heroui function directly so it can be called by Tailwind.
 */
export default heroui;
