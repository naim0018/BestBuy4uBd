/**
 * RecommendedForYou – wraps the existing Home component RecommendedSection
 * so it can be imported from the shared /components directory while keeping
 * the original implementation in place.
 */
export { default } from "@/pages/Public/Home/Home1/Components/Recommended/RecommendedSection";
